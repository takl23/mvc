{% extends "base.html.twig" %}
{% block title %}Card Game{% endblock %}

{% block body %}

{{ include('flash.html.twig') }}

<div class="playrow">

<div class="play2">
<h1>Player 1</h1>
  
    {% for card in player1Hand %}
        {{ card.representCard() }}
    {% endfor %}
  
<p> Score: {{ player1Score }}</p>
</div>


<div class="play2">
<h1>Player 2:</h1>
    {% for card in player2Hand %}
        {{ card.representCard() }}
    {% endfor %}
    
<p> Score: {{ player2Score }}</p>
</div>
</div>

<div class="playrow">
<form action="{{ path('init21') }}" method="post">
    <button type="submit">Nytt spel</button>
</form>
</div>

{% endblock %}